# Example environment configuration for Supabase

# Database Type - can be "sqlite", "supabase", or "inmemory"
DB_TYPE=supabase

# Supabase Configuration (required when DB_TYPE=supabase)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-anon-public-api-key

# Server Configuration
SERVER_PORT=:8080
LOG_LEVEL=info
LOG_FILE=server.log

# SQLite Configuration (used when DB_TYPE=sqlite)
DB_PATH=./blog.db
# JWT Authentication Configuration
JWT_SECRET=your-secret-key-change-this-in-production
JWT_TOKEN_DURATION_HOURS=24
